module Numav
    using CxxWrap
    using Libdl

    function _get_lib_path()
        libname = Sys.iswindows() ? "numav_jl" : "libnumav_jl"
        return joinpath(@__DIR__, "install", "lib", libname)
    end

    @wrapmodule(_get_lib_path)

    function __init__()
        @initcxx
    end
end