# sala_config_05_PRATO

## Objetivo
Descrever a configuração do caso (TCC - PRATO) e manter rastreabilidade do pipeline de simulação.

## Inputs
- `inputs/`: malhas (.bdf) e demais arquivos necessários para rodar a simulação.

## Outputs
- `outputs/`: resultados gerados (ignorados pelo Git, exceto `.gitkeep`).

## Como rodar
(será preenchido após integrar o build da simulação no CMake)

> Nota: arquivos .bdf nao sao versionados. Gere a malha e coloque em `inputs/`.
