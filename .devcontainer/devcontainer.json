// Copyright (c) 2026 Matheus Machado Fiuza <matheusmachadofiuza@gmail.com>

// Configures VS Code's Dev Containers extension: uses docker-compose, attaches
// to the 'numav' service, sets the workspace folder, runs initialize.sh before
// creation, installs VS Code extensions, and specifies the remote user.

{
  "name": "Numav Development Container",
  // Use your existing compose file
  "dockerComposeFile": "../docker/docker-compose.yml",
  // Tell VS Code which service to attach to
  "service": "numav",
  // The working folder inside the container
  "workspaceFolder": "/workspace",
  
  "overrideCommand": false,

  // Run this script on the host *before* the container is created
  "initializeCommand": "${localWorkspaceFolder}/.devcontainer/initialize.sh",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cpptools-extension-pack",
        "ms-vscode.cpp-devtools",
        "jeff-hykin.better-cpp-syntax",
        "julialang.language-julia",
        "twxs.cmake",
        "ms-vscode.cmake-tools",
        "bertrand-thierry.vscode-gmsh",
        "aaron-bond.better-comments",
        "editorconfig.editorc",
        "mechatroner.rainbow-csv"
      ]
    }
  },

  // This matches the user created by entrypoint.sh.
  "remoteUser": "devuser"
}
